{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "buildpreview": "npx wrangler dev .output/server/index.mjs --site .output/public --local",
    "deploy": "npx wrangler publish"
  },
  "devDependencies": {
    "nuxt": "npm:nuxt3@latest",
    "wrangler": "^2.1.4"
  },
  "dependencies": {
    "@picocss/pico": "^1.5.5"
  }
}
